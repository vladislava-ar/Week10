Вопросы 💎

1. Какие есть способы объявления функций?

1) Function Declaration (основной)
2) Function Expression
3) Стрелочные функции (самый новый)

2. Приведите примеры вызова одной и той же функции всеми известными вам способами.

Function Declaration 
    function sum(a, b) {
    return a + b; 
    }

Function Expression 
    let sum = function (a, b) {
    return a + b;
};

Стрелочная функция 
    let sum = (a,b) => a + b;

3. В чём разница между тестированием и отладкой (дебаггингом)? А что такое логирование? 

Отладка – это процесс поиска и исправления ошибок в скрипте. Это можно осуществить непосредственно в браузере, они поддерживают инструменты для отладки. 
Тестирование – это проверка соответствия между реальным и ожидаемым поведением программы. Выполняется ручным (проверка функционала буквально нажатием на каждую кнопку, вводом данных в каждое поле и т.д.) или автоматизированным методом (посредством специально написанных автотестов под конкретный код). 
Логирование – это вывод данных в консоль из кода функцией console.log.

4. В чём разница между Function Expression и Function Declaration? 

Если функция объявлена как отдельная инструкция в основном потоке кода, то это Function Declaration. Если функция была создана как часть выражения, то считается, что эта функция объявлена при помощи Function Expression. 
В Function Expression ставится точка с запятой(;) на конце, а в Function Declaration нет. Function Expression создаётся, когда выполнение доходит до нее, и затем уже может использоваться. Function Declaration можно использовать во всем скрипте (или блоке кода, если функция объявлена в блоке). 

5. Что делает функция `console.log()`?

Функция console.log. применяется, если нужно что-то вывести в консоль из кода. 

6. По какой причине, мы можем использовать до объявления функцию, объявленную через `Function Declaration`, но не можем ту, которая объявлена через `Function Expression` ?

Function Declaration можно вызвать до ее объявления, потому что движок выполняет код не построчно сверху вниз, он сначала его компилирует, а лишь затем интерпретирует. При первом проходе компилятор найдёт все объявления функций и объявит их. При этом никакой код не выполняется, методы не вызываются. Затем найдёт все переменные, объявленные через `var`, объявит их и присвоит каждой значение `undefined`.

7. Что такое глобальная зона видимости функций?

Область видимости вне любых функций называется глобальной. Переменные, объявленные в ней, видны везде. 

8. Что вернёт код? 
    
    function showX(x)
    { 
        return x;
    }
    console.log(showX(28));

Код вернет число 28. 
    
9. Что выведет этот код?
    
    function sum(x, y=0) {
        return x+y;
    } 
    console.log(sum(5));
    
Код выведет число 5. 

10. Что выведет код? 

    console.log(foo); 

    let foo = "Cat";

    console.log(foo); 

    Код выведет ошибку: Uncaught ReferenceError: Cannot access 'foo' before initialization.